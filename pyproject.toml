[project]
name = "yasdef-worker"
version = "0.2.2"
description = "Python orchestrator for the YASDEF worker workflow"
readme = "Readme.md"
requires-python = ">=3.11"
dependencies = [
    "PyYAML>=6.0",
]

[project.scripts]
yasdef = "yasdef_worker.cli.main:main"

[project.optional-dependencies]
dev = [
    "hypothesis>=6.0",
    "mypy>=1.10",
    "pytest>=8.0",
    "ruff>=0.6",
    "types-PyYAML>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/yasdef_worker"]
exclude = [
    "src/yasdef_worker/_data",
    "src/yasdef_worker/templates",
]

[tool.hatch.build.targets.wheel.force-include]
"src/yasdef_worker/_data" = "yasdef_worker/_data"
"src/yasdef_worker/templates" = "yasdef_worker/templates"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests/unit", "tests/integration"]

[tool.mypy]
python_version = "3.11"
warn_unused_configs = true

[[tool.mypy.overrides]]
module = "yasdef_worker.domain.*"
strict = true

[tool.ruff]
line-length = 100
target-version = "py311"
